import type { PageServerLoad } from "./$types";

export const load: PageServerLoad = async () => ({
	title: "Custom Monika After Story Content",
	description: "📗 Community-maintained listing of MAS custom content"
});
